export type userInput = {
    name: string,
    email: string,
    password: string
}

export type userData = {
    id: string,
    name: string,
    email: string,
    password: string
}

export type productsInsert = {
    name: string,
    price: number,
    image_url: string
}

export type productsData = {
    id: string,
    name: string,
    price: number,
    image_url: string
}