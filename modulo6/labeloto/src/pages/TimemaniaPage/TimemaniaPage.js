import React from "react"
import { Button } from "@mui/material"
import { MainContainer } from "./styled"

const TimemaniaPage = () => {

    return (
        <MainContainer>
            <Button variant="contained" color="primary">Timemania</Button>
        </MainContainer>
    )
}

export default TimemaniaPage