import React from "react"
import { Button } from "@mui/material"
import { MainContainer } from "./styled"

const QuinaPage = () => {

    return (
        <MainContainer>
            <Button variant="contained" color="primary">Quina</Button>
        </MainContainer>
    )
}

export default QuinaPage