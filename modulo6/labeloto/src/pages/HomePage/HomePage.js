import React from "react"
import { Button } from "@mui/material"
import { MainContainer } from "./styled"

const HomePage = () => {

    return (
        <MainContainer>
            <Button variant="contained" color="primary">LABELOTO</Button>
        </MainContainer>
    )
}

export default HomePage